import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ItemSepratore = ({ width, height }) => {
  return <View style={{ width, height }}></View>;
};

export default ItemSepratore;

const styles = StyleSheet.create({});
